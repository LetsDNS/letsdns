# Activate the previously created Python virtual environment
# and change to your local Git repository clone.
source /path/to/LetsDNS/.venv/bin/activate
cd /path/to/LetsDNS/letsdns

# Launch LetsDNS.
PYTHONPATH=. python letsdns/main.py /path/to/your.conf
