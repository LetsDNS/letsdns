# Download install script using cURL, wget, or similar.
curl -fso install.sh https://raw.githubusercontent.com/LetsDNS/letsdns/master/scripts/install.sh

# Optional: Verify that the downloaded script does not
# do anything nasty, using Mark I Eyeball checks.
cat install.sh

# Mark downloaded script as executable.
chmod 0700 install.sh

# Execute script, modifying the destination directory
# according to personal taste/needs.
./install.sh /var/lib/letsdns
